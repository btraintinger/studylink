import { Typography } from '@mui/material';
import Layout from '../../../components/page/layout';

export default function SchoolClasses() {
  return (
    <Layout role="ADMIN">
      <Typography>SchoolClasses</Typography>
    </Layout>
  );
}
